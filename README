# Task
Data Engineer Task of Snapp! Express

## Requirements
* Install spark for spark submit
* Install and Run Minio and Load data
* Install project requirements
```bash
pip install -r requirements.txt
```

## Run
```bash
cd runner
chmod +x etl_runner.sh
./etl_runner.sh
```
